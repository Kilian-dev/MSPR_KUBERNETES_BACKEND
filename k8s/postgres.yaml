apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: cofrap-db
  namespace: cofrap
spec:
  instances: 1
  storage:
    size: 2Gi
  bootstrap:
    initdb:
      database: cofrap
      owner: cofrap_user
